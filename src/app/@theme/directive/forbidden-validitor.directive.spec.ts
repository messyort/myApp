import { ForbiddenValiditorDirective } from './forbidden-validitor.directive';

describe('ForbiddenValiditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenValiditorDirective();
    expect(directive).toBeTruthy();
  });
});
