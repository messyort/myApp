import { ConfirmValiditorDirective } from './confirm-validitor.directive';

describe('ConfirmValiditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmValiditorDirective();
    expect(directive).toBeTruthy();
  });
});
